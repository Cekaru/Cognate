// Package semantic implements the L2 cache tier: pgvector similarity search
// over BGE-M3 query embeddings, gated by a per-language-pair threshold and the
// structural token guard.
package semantic
